# FACTER: requirements.txt
# Core dependencies
torch>=2.1
transformers>=4.30
sentence-transformers>=2.2
scikit-learn>=1.0
datasets>=2.12
numpy>=1.20
pandas>=1.3
tqdm>=4.62
requests>=2.25
psutil>=5.8
python-dotenv>=0.21
accelerate==1.10.1
codecarbon>=3.2
jinja2>=3.1.0
sentencepiece
protobuf
tiktoken

# Optional/Recommended for research and development
# rich>=13.0           # Advanced logging (optional)
# loguru>=0.7          # Advanced logging (optional)
# jupyter              # Jupyter notebook support (optional)
# platformdirs         # Mac/Windows compatibility (optional)
# scipy>=1.7           # Advanced metrics (optional)

# Environment and compatibility notes (not for pip, for user reference only):
# - difflib: standard library, no need to install
# - If using GPU, ensure CUDA toolkit is installed
# - For LLMs, ensure enough RAM/VRAM
# - pip>=21.0 recommended
# - python>=3.8 recommended

# End of requirements